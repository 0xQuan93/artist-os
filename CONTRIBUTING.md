# Contributing

Use Node.js 20 or newer. Before opening a pull request, run:

```bash
npm run check
```

Keep the core offline-capable, optional integrations disabled by default, and
artist-private material out of fixtures, screenshots, logs, and commits. Add a
test for behavior changes. Contributions must not imply artist approval,
publication, minting, rights ownership, or endorsement.
